{% extends 'base.html.twig' %}

{% block title %}Edit Movie{% endblock %}

{% block body %}
    <h1 style="text-align: center">Edit Movie</h1>





    {{ include('movie/_form.html.twig',  {'button_label': 'Update'}  ) }}

    <br>
    <div class="container text-center" style="display: flex;margin: 0 auto 0 auto">
        <div class="container">
    <button class="btn btn-secondary" onclick="location.href='{{ path('app_movie_index') }}'">Back to list</button>
        </div>
        <div class="container">
    {{ include('movie/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
